// Restored from ref/webview/assets/app-intl-signal-Bd_tJ6VJ.js

import { signal } from "@preact/signals-react";
import { createIntl } from "react-intl";

export const appIntlSignal = signal(
  createIntl({
    locale: "en",
    messages: {},
  }),
);
