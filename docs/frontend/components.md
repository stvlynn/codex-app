# Components

- One component per file unless they are tightly coupled variants.
- Export the component as named export unless the file is a barrel.
- Props must be typed with an explicit TypeScript interface.
- Use semantic class names; avoid cryptic single-letter identifiers.
- Do not hardcode user-facing strings; reference constants or i18n keys.
