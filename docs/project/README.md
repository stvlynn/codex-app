# Project

This section describes the `decode-codex` project at a high level.

## Documents

- [`overview.md`](overview.md) — what the project is, its goals, and non-goals.
- [`architecture.md`](architecture.md) — system architecture, module boundaries, and data flow.

## What belongs here

- Project goals and non-goals.
- High-level architecture and module relationships.
- Technology-stack decisions.
- Cross-cutting concerns that touch both restored UI and pipeline scripts.

## What does not belong here

- Detailed layer rules for restored UI (those live in [`docs/frontend/`](../frontend/README.md)).
- Pipeline layer rules (those live in [`docs/backend/`](../backend/README.md)).
- Operational procedures (those live in [`docs/operations/`](../operations/README.md)).
- Testing or code-review policy (those live in [`docs/quality/`](../quality/README.md)).
